export const publicRoutes = [
    "/"
]

export const authRoutes = [
    "/auth/signin",
    "/register"
]

export const apiAuthPrefix = "/api/auth";

export const defaultLoginRedirect = "/userdashboard"